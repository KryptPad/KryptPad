<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.MainPage"
    mc:Ignorable="d">


    <Page.DataContext>
        <Models:MainPageViewModel/>
    </Page.DataContext>

    <Grid>
        <SplitView x:Name="ShellSplitView" DisplayMode="CompactOverlay"
		IsPaneOpen="{Binding IsPaneOpen}" CompactPaneLength="48" OpenPaneLength="200"
		PaneBackground="{ThemeResource SplitViewBackgroundBrush}">
            <SplitView.Pane>
                <StackPanel>
                    <!-- Menu (hamburger) button -->
                    <RadioButton Style="{StaticResource SplitViewMenuButtonStyle}" Command="{Binding MenuButtonCommand}" IsChecked="{Binding IsMenuButtonChecked}" />

                    <!-- Home button -->
                    <RadioButton Tag="&#xE10F;" Content="Home" Style="{StaticResource SplitViewNavButtonStyle}" IsChecked="True" Command="{Binding HomeNavButtonCommand}" />

                    <!-- Search button -->
                    <RadioButton Tag="&#xE11A;" Content="Search" Style="{StaticResource SplitViewNavButtonStyle}" />

                    <!-- Settings button -->
                    <RadioButton Tag="&#xE115;" Content="Settings" Style="{StaticResource SplitViewNavButtonStyle}"  />

                    <!-- About button -->
                    <RadioButton Tag="&#xE783;" Content="About" Style="{StaticResource SplitViewNavButtonStyle}" />
                </StackPanel>
            </SplitView.Pane>
        </SplitView>

        <!---->
        <Popup x:Uid="LoginPopup" IsOpen="True" Name="LoginPopup" HorizontalAlignment="Stretch"
               Loaded="LoginPopup_Loaded">
            <Border Background="{StaticResource ApplicationPageBackgroundThemeBrush}"
                    BorderThickness="2"
                    BorderBrush="{StaticResource ApplicationForegroundThemeBrush}">
                <Grid  HorizontalAlignment="Stretch" >
                    <Button>This is a test button</Button>
                </Grid>
            </Border>
        </Popup>

    </Grid>
    
</Page>
