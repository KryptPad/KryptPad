<UserControl
    x:Class="KryptPadCSApp.UserControls.BusyIndicator"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.UserControls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    d:DesignHeight="300"
    d:DesignWidth="400">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <!--Indicator {Binding IsBusy}-->
        <ProgressRing x:Name="BusyProgressRing" 
                      Grid.Column="0" 
                      Foreground="{StaticResource TextForegroundBrush}"
                      HorizontalAlignment="Center"
                      VerticalAlignment="Center"
                      Margin="0,0,10,0"/>

        <!--Text-->
        <TextBlock Text="Signing you in" Grid.Column="1"
                   Foreground="{StaticResource TextForegroundBrush}"/>
    </Grid>
</UserControl>
