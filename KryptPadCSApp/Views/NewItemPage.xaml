<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.Views.NewItemPage"
    mc:Ignorable="d"
    Name="AddNewItemPage">

    <Page.DataContext>
        <Models:NewItemPageViewModel/>
    </Page.DataContext>

    <Page.BottomAppBar>
        <CommandBar>
            <AppBarButton Icon="Accept" Label="Done" Command="{Binding AddItemCommand}" />
            <AppBarButton Icon="Cancel" Label="Cancel" Command="{Binding CancelCommand}" />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}" Margin="10">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="0,0,0,10">

            <TextBlock Text="{Binding Category.Name}" FontSize="{ThemeResource SplitViewLargeNavFontSize }" />

            <TextBlock Text="Item name" />
            <TextBox Text="{Binding ItemName, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" 
                     HorizontalAlignment="Left"
                     Width="250"/>

            <!--<TextBlock Text="Item type" />
            <ComboBox ItemsSource="{Binding ItemTypes}"
                      SelectedItem="{Binding SelectedItem, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>-->

        </StackPanel>

        <!--Visibility="{Binding FieldsVisibility}"-->
        <Grid
              Grid.Row="1"
              HorizontalAlignment="Stretch"
              >
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" 
                        HorizontalAlignment="Left">
                <!--Field list-->
                <TextBlock TextWrapping="WrapWholeWords">
                        Add fields to this profile (e.g. Username, Password, etc...)
                </TextBlock>
                <Button Command="{Binding AddFieldCommand}">Add Field</Button>
            </StackPanel>


            <!--Split this row into two columns with a grid, the right side will be for notes, and the left side for custom user fields-->
            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <!--Custom fields-->
                <GridView ItemsSource="{Binding Fields}"
                          HorizontalAlignment="Stretch"
                          ScrollViewer.HorizontalScrollMode="Auto"
                          Grid.Column="0"
                          Grid.Row="0">
                    <GridView.ItemsPanel>
                        <ItemsPanelTemplate>
                            <ItemsWrapGrid Orientation="Vertical" />

                        </ItemsPanelTemplate>
                    </GridView.ItemsPanel>

                </GridView>

                <!--Notes-->
                
                <TextBox Grid.Column="1"
                         Grid.Row="0"
                         x:Name="NotesTextBox"
                         
                         Text="{Binding Notes, Mode=TwoWay}"
                         AcceptsReturn="True"/>

                
                
            </Grid>
            

        </Grid>


        <!--<Grid Visibility="{Binding NotesVisibility}"
              Grid.Row="1">

            <TextBox Text="{Binding Notes, Mode=TwoWay}"
                     AcceptsReturn="True"/>
        </Grid>-->
        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="VisualStatePhone">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesTextBox.Width" Value="100" />
                        <Setter Target="NotesTextBox.Grid.Row" Value="1" />
                        <Setter Target="NotesTextBox.Grid.Column" Value="0" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateTablet">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="600"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="NotesTextBox.Width" Value="600" />
                        <Setter Target="NotesTextBox.Grid.Row" Value="0" />
                        <Setter Target="NotesTextBox.Grid.Column" Value="1" />
                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
