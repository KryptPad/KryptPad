<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.Views.SelectProfilePage"
    mc:Ignorable="d"
    x:Name="SelectProfileViewPage" Loaded="SelectProfileViewPage_Loaded">

    <Page.DataContext>
        <Models:SelectProfilePageViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">

        <!--Main grid for page-->
        <Grid HorizontalAlignment="Stretch"
			MaxWidth="400"
			Padding="12">


            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Profiles" Style="{ThemeResource HeaderTextBlockStyle}" />
                <!--<TextBlock Grid.Row="1" Text="Choose a profile or create a new one" TextWrapping="WrapWholeWords"
                           Margin="0,0,0,12"/>-->
                
                <!--Choose profile-->
                <ComboBox Grid.Row="1"
                          HorizontalAlignment="Stretch"
                          ItemsSource="{Binding Profiles}"
                          SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                          Margin="0,0,0,12">
                    <ComboBox.ItemTemplate>
                        <DataTemplate>

                            <Grid x:Name="ProfileGrid" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                
                                <!--A nice little icon-->
                                <SymbolIcon Grid.Column="0" Symbol="ProtectedDocument" Margin="0,0,12,0" />
                                
                                <!--Profile name-->
                                <TextBlock Text="{Binding Name}"
                                           Grid.Column="1"
                                           VerticalAlignment="Center"/>

                            </Grid>

                        </DataTemplate>
                    </ComboBox.ItemTemplate>
                </ComboBox>
                
                <!--Passphrase grid-->
                <Grid Grid.Row="2"
                      Margin="0,0,0,12">

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>
                    
                    <!--Enter passphrase-->
                    <PasswordBox x:Name="PassphrasePasswordBox"
                                 Grid.Column="0"
                                 Password="{Binding Passphrase, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                                 PlaceholderText="Enter passphrase" KeyUp="PassphrasePasswordBox_KeyUp"/>

                    <Button Grid.Column="1"
                            Command="{Binding EnterProfileCommand}">
                        <SymbolIcon Symbol="Accept" />
                    </Button>
                </Grid>

                <!--Create or restore profile-->
                <StackPanel Grid.Row="3"
                            Margin="0,0,0,24"
                            Orientation="Horizontal">
                    <Button Content="Create Profile"
                            Command="{Binding CreateProfileCommand}"
                            Margin="0,0,12,0"/>

                    <Button Content="Restore Backup"
                            Command="{Binding RestoreBackupCommand}"/>
                </StackPanel>

                <!--<ListBox Grid.Row="3"
                         ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile, Mode=TwoWay}">

                    <ListBox.Background>
                        <SolidColorBrush Color="#FFDFEEF7" Opacity="0.5"/>
                    </ListBox.Background>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate >
                                        <Button x:Name="button" HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Stretch"
                                                Command="{Binding DataContext.SelectProfileCommand, ElementName=SelectProfileViewPage}"
                                                CommandParameter="{Binding}">
                                            <ContentPresenter />

                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="BorderThickness" Value="0" />
                                                    <Setter Property="Padding" Value="8" />
                                                </Style>
                                            </Button.Style>

                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal" />
                                                    <VisualState x:Name="PointerOver">
                                                        <VisualState.Setters>
                                                            <Setter Target="button.(Control.Background)" Value="{ThemeResource SystemControlHighlightListAccentHighBrush}"/>
                                                        </VisualState.Setters>

                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <VisualState.Setters>
                                                            <Setter Target="button.(Control.Background)" Value="{ThemeResource SystemControlHighlightListMediumBrush}"/>

                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>

                                        </Button>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>

                            <Grid x:Name="ProfileGrid" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>

                                --><!--A nice little icon--><!--
                                <SymbolIcon Grid.Column="0" Symbol="ProtectedDocument" Margin="0,0,12,0" />

                                --><!--Profile name--><!--
                                <TextBlock Text="{Binding Name}"
                                           Grid.Column="1"
                                           VerticalAlignment="Center"/>


                            </Grid>

                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>-->

            </Grid>

        </Grid>


    </Grid>
</Page>
