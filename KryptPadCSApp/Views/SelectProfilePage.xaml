<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.Views.SelectProfilePage"
    mc:Ignorable="d"
    x:Name="SelectProfileViewPage">

    <Page.DataContext>
        <Models:SelectProfilePageViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">

        <!--Main grid for page-->
        <Grid HorizontalAlignment="Stretch"
			MaxWidth="400"
			Padding="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>


                <Grid Grid.Row="0">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="auto" />
                        <RowDefinition Height="*" />
                        <RowDefinition Height="auto" />
                    </Grid.RowDefinitions>

                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="*" />
                        
                    </Grid.ColumnDefinitions>

                    <!--<Button Grid.Row="0"
                            Grid.Column="0"
                            Command="{Binding SignOutCommand}">
                        <SymbolIcon Symbol="Back" />
                    </Button>-->

                    <TextBlock Grid.Row="0" Grid.Column="1" Text="Profiles" Style="{StaticResource LargeBlueTextBlock}" />
                    <TextBlock Grid.Row="1" Grid.ColumnSpan="2" Text="Choose a profile or create a new one" TextWrapping="WrapWholeWords"  />
                    <Button Grid.Row="2"
                            Grid.ColumnSpan="2"
                            Content="Create Profile"
                            Command="{Binding CreateProfileCommand}"/>
                </Grid>

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Profiles}"
                         SelectedItem="{Binding SelectedProfile, Mode=TwoWay}">

                    <ListBox.Background>
                        <SolidColorBrush Color="#FFDFEEF7" Opacity="0.5"/>
                    </ListBox.Background>

                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">

                            <Setter Property="Template">
                                <Setter.Value>
                                    <ControlTemplate >
                                        <Button x:Name="button" HorizontalAlignment="Stretch"
                                                HorizontalContentAlignment="Stretch"
                                                Command="{Binding DataContext.SelectProfileCommand, ElementName=SelectProfileViewPage}"
                                                CommandParameter="{Binding}">
                                            <ContentPresenter />

                                            <Button.Style>
                                                <Style TargetType="Button">
                                                    <Setter Property="BorderThickness" Value="0" />
                                                    <Setter Property="Padding" Value="8" />
                                                </Style>
                                            </Button.Style>

                                            <VisualStateManager.VisualStateGroups>
                                                <VisualStateGroup x:Name="CommonStates">
                                                    <VisualState x:Name="Normal" />
                                                    <VisualState x:Name="PointerOver">
                                                        <VisualState.Setters>
                                                            <Setter Target="button.(Control.Background)" Value="{ThemeResource SystemControlHighlightListAccentHighBrush}"/>
                                                        </VisualState.Setters>

                                                    </VisualState>
                                                    <VisualState x:Name="Pressed">
                                                        <VisualState.Setters>
                                                            <Setter Target="button.(Control.Background)" Value="{ThemeResource SystemControlHighlightListMediumBrush}"/>

                                                        </VisualState.Setters>
                                                    </VisualState>
                                                </VisualStateGroup>
                                            </VisualStateManager.VisualStateGroups>

                                        </Button>
                                    </ControlTemplate>
                                </Setter.Value>
                            </Setter>

                        </Style>
                    </ListBox.ItemContainerStyle>

                    <ListBox.ItemTemplate>
                        <DataTemplate>

                            <Grid x:Name="ProfileGrid" >
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="auto" />
                                    <ColumnDefinition Width="auto" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Text="{Binding Name}"
                                           Grid.Column="0"
                                           VerticalAlignment="Center"/>

                                <!--<Button Grid.Column="1"
                                        Command="{Binding DataContext.RenameProfileCommand, ElementName=SelectProfileViewPage}"
                                        CommandParameter="{Binding}">
                                    <SymbolIcon Symbol="Rename" />
                                </Button>

                                <Button Grid.Column="2"
                                        Command="{Binding DataContext.DeleteProfileCommand, ElementName=SelectProfileViewPage}"
                                        CommandParameter="{Binding}">
                                    <SymbolIcon Symbol="Delete" />
                                </Button>-->
                            </Grid>

                        </DataTemplate>
                    </ListBox.ItemTemplate>

                </ListBox>


                

            </Grid>

        </Grid>


    </Grid>
</Page>
