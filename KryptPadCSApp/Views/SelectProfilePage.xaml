<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.Views.SelectProfilePage"
    mc:Ignorable="d">

    <Page.DataContext>
        <Models:SelectProfilePageViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource WhiteToBlueBrush}">
        <!--Main grid for page-->
        <Grid VerticalAlignment="Center"
			HorizontalAlignment="Stretch"
			MaxWidth="400"
			Padding="10">

            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>


                <StackPanel Grid.Row="0">
                    <TextBlock Grid.Row="0" Text="Profiles" Style="{StaticResource LargeBlueTextBlock}" />
                    <TextBlock Text="Choose a profile or create a new one" TextWrapping="WrapWholeWords" Margin="0,0,0,10" />

                </StackPanel>

                <ListBox Grid.Row="1" ItemsSource="{Binding Profiles}">
                    <ListBox.Background>
                        <SolidColorBrush Color="#FFDFEEF7" Opacity="0.5"/>
                    </ListBox.Background>

                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <TextBlock Text="{Binding Name}" />
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                    
                </ListBox>
            </Grid>

        </Grid>
    </Grid>
</Page>
