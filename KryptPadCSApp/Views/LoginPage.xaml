<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.Views.LoginPage"
    mc:Ignorable="d">

    <Page.DataContext>
        <Models:LoginPageViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource WhiteToBlueBrush}" >

        <!--Main grid for page-->
        <Grid VerticalAlignment="Center"
              HorizontalAlignment="Stretch"
              MaxWidth="400"
              Padding="10">

            <!--Main grid for input-->
            <Grid Visibility="{Binding LoginVisibility}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           FontSize="64"
                           Margin="0,0,0,10"
                           Foreground="{StaticResource BlueTextForegroundBrush}"
                           Text="Krypt Pad">

                </TextBlock>

                <!--Login email address-->
                <TextBox Grid.Row="1" PlaceholderText="Email Address" Margin="0,0,0,10"
                         InputScope="EmailNameOrAddress"
                         Text="{Binding Email, Mode=TwoWay}" />

                <!--Password-->
                <PasswordBox Grid.Row="2" PlaceholderText="Password" Margin="0,0,0,10" 
                         Password="{Binding Password, Mode=TwoWay}"/>

                <!--Remember me-->
                <CheckBox Grid.Row="3" Content="Sign me in automatically"  Margin="0,0,0,10" />

                <!--Login / Sign up buttons-->
                <Grid Grid.Row="4" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <Button Content="Log In" Grid.Column="0" Margin="0,0,10,0"
                        Command="{Binding LogInCommand}" />

                    <Button Content="Create Account" Grid.Column="1" 
                            Command="{Binding CreateAccountCommand}" />

                </Grid>


            </Grid>

            <!--Progress indicator-->
            <Grid Visibility="{Binding BusyIndicatorVisibility}">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <!--Indicator {Binding IsBusy}-->
                <ProgressRing Grid.Column="0" IsActive="true" 
                              Foreground="{StaticResource BlueTextForegroundBrush}"
                              HorizontalAlignment="Center"
                              VerticalAlignment="Center"
                              Margin="0,0,10,0"/>

                <!--Text-->
                <TextBlock Text="Signing you in" Grid.Column="1"
                           Foreground="{StaticResource BlueTextForegroundBrush}"/>
            </Grid>


        </Grid>

        

    </Grid>
</Page>
