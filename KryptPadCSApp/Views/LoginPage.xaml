<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    xmlns:UserControls="using:KryptPadCSApp.UserControls"
    x:Class="KryptPadCSApp.Views.LoginPage"
    mc:Ignorable="d">

    
    <Page.DataContext>
        <Models:LoginPageViewModel/>
    </Page.DataContext>

    <Grid Background="{StaticResource AppBackgroundBrush}" >

        <!--Main grid for page-->
        <Grid x:Name="MainGrid" VerticalAlignment="Top"
			HorizontalAlignment="Stretch"
			MaxWidth="400"
			Padding="10">

            <!--Main grid for input-->
            <Grid Visibility="{Binding LoginVisibility}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Krypt Pad" Style="{StaticResource LargeBlueTextBlock}"/>

                <!--Login email address-->
                <TextBox Grid.Row="1" PlaceholderText="Email Address" Margin="0,0,0,10"
					InputScope="EmailNameOrAddress"
					Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                <!--Password-->
                <PasswordBox Grid.Row="2" PlaceholderText="Password" Margin="0,0,0,10" 
					Password="{Binding Password, Mode=TwoWay}"/>

                <!--Remember me-->
                <CheckBox IsChecked="{Binding AutoSignIn, Mode=TwoWay}" Grid.Row="3" Content="Sign me in automatically"  Margin="0,0,0,10" />

                <!--Login / Sign up buttons-->
                <Grid Grid.Row="4" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <Button Content="Sign In" Grid.Column="0" Margin="0,0,10,0"
						Command="{Binding LogInCommand}" />

                    <Button Content="Sign Up for Free" Grid.Column="1" 
						Command="{Binding CreateAccountCommand}" />

                </Grid>


            </Grid>

        </Grid>

        <!--Progress indicator-->
        <ProgressRing Visibility="{Binding BusyIndicatorVisibility}"
                      IsActive="{Binding IsBusy}"
                      Width="50"
                      Height="50"
                      Foreground="White"/>



        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="VisualStatePhone">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>

                        <Setter Target="MainGrid.VerticalAlignment" Value="Top" />
                        
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateTablet">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1024"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="MainGrid.VerticalAlignment" Value="Center" />

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
