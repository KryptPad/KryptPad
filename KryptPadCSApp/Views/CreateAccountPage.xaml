<Page
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    xmlns:UserControls="using:KryptPadCSApp.UserControls"
    x:Class="KryptPadCSApp.Views.CreateAccountPage"
    mc:Ignorable="d">

    <Grid Background="{StaticResource AppBackgroundBrush}">

        <Grid.DataContext>
            <Models:CreateAccountPageViewModel/>
        </Grid.DataContext>

        <!--Main grid for page-->
        <Grid x:Name="MainGrid"
              VerticalAlignment="Top" 
              HorizontalAlignment="Stretch"
			  MaxWidth="400"
			  Padding="10">
            
            

            <Grid Visibility="{Binding AccountInfoVisibility}">
                <Grid.RowDefinitions>
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                    <RowDefinition Height="auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0" Text="Create Account" Style="{StaticResource LargeBlueTextBlock}" />
                
                <TextBlock Grid.Row="1"
					Margin="0,0,0,10"
					TextWrapping="WrapWholeWords">
					<Run Text="Before you can use KryptPad, you must create an account with kryptpad.com."/>
                </TextBlock>

                <!--Login email address-->
                <TextBox Grid.Row="2" PlaceholderText="Email Address" Margin="0,0,0,10"
					InputScope="EmailNameOrAddress"
					Text="{Binding Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!--Password-->
                <PasswordBox Grid.Row="3" PlaceholderText="Password" Margin="0,0,0,10" 
					Password="{Binding Password, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>

                <!--Password-->
                <PasswordBox Grid.Row="4" PlaceholderText="Confirm Password" Margin="0,0,0,10" 
					Password="{Binding ConfirmPassword, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"/>


                <Grid Grid.Row="5" Margin="0,0,0,10">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="auto" />
                        <ColumnDefinition Width="auto" />
                    </Grid.ColumnDefinitions>

                    <Button Content="Create Account" Grid.Column="0" Margin="0,0,10,0" 
                            Command="{Binding CreateAccountCommand}"/>

                </Grid>


            </Grid>

            
        </Grid>
        <!--Progress indicator-->
        <ProgressRing Visibility="{Binding BusyIndicatorVisibility}"
                      IsActive="{Binding IsBusy}"
                      Width="50"
                      Height="50"
                      Foreground="White"/>


        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="VisualStatePhone">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>

                        <Setter Target="MainGrid.VerticalAlignment" Value="Top" />

                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateTablet">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1024"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                        <Setter Target="MainGrid.VerticalAlignment" Value="Center" />

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
        
    </Grid>
</Page>
