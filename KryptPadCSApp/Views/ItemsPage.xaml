<Page xmlns:UserControls="using:KryptPadCSApp.UserControls" 
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:KryptPadCSApp.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:Models="using:KryptPadCSApp.Models"
    x:Class="KryptPadCSApp.Views.ItemsPage"
    mc:Ignorable="d"
    xmlns:Commanding="using:Commanding"
    x:Name="ItemsViewPage">

    <Page.DataContext>
        <Models:ItemsPageViewModel/>
    </Page.DataContext>

    <Page.BottomAppBar>
        <CommandBar Visibility="{Binding BottomAppBarVisible}">
            <AppBarButton Icon="Delete" Label="Delete Item" Command="{Binding DeleteItemCommand}" />
        </CommandBar>
    </Page.BottomAppBar>

    <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!--Command bar-->
        <Grid Grid.Row="0" Background="{ThemeResource AppBarBackgroundThemeBrush}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="auto" />
                <ColumnDefinition Width="auto" />
            </Grid.ColumnDefinitions>

            <CommandBar Grid.Column="1">
                <AppBarButton Icon="SelectAll" Label="Select Items" Command="{Binding ToggleSelectionMode}" />
                <AppBarButton Icon="Add" Label="Add Category" Command="{Binding AddCategoryCommand}" />
                <AppBarButton Icon="Permissions" Label="Change Password" />

                <CommandBar.SecondaryCommands>
                    <AppBarButton Icon="SaveLocal" Label="Download Profile..." />
                </CommandBar.SecondaryCommands>
            </CommandBar>

        </Grid>

        <!--Search and items-->
        <Grid Grid.Row="1" Margin="12">
            <Grid.RowDefinitions>
                <RowDefinition Height="auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>


            <!--Search box-->
            <TextBox x:Name="SearchTextBox" Grid.Row="0" PlaceholderText="Search..." 
                     Text="{Binding SearchText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                     HorizontalAlignment="Left"
                     Width="250"/>

            <!--Items grid-->
            <GridView Grid.Row="1"
                  
                  ItemsSource="{Binding ItemsView.View}"
                  SelectionMode="{Binding DataContext.SelectionMode, ElementName=ItemsViewPage}"
                  IsItemClickEnabled="{Binding DataContext.IsItemClickEnabled, ElementName=ItemsViewPage}"
                  SelectedItem="{Binding DataContext.SelectedItem, ElementName=ItemsViewPage, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}"
                  Commanding:GridViewItemClickCommand.Command="{Binding DataContext.ItemClickCommand, ElementName=ItemsViewPage}"
                  ItemTemplate="{StaticResource GrivViewItemsTemplate }">

                <GridView.GroupStyle>
                    <GroupStyle>
                        <GroupStyle.HeaderTemplate>
                            <DataTemplate>
                                <Grid Background="{StaticResource ApplicationPageBackgroundThemeBrush}">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="auto" />
                                    </Grid.ColumnDefinitions>

                                    <!--Trigger add item with the category as the parameter-->
                                    <Button Grid.Column="0" 
                                        Command="{Binding DataContext.AddItemCommand, ElementName=ItemsViewPage }"
                                        CommandParameter="{Binding}">
                                        <SymbolIcon Symbol="Add" />
                                    </Button>

                                    <!--Name of category-->
                                    <TextBlock Grid.Column="1" 
                                           Text='{Binding Name}' 
                                           Foreground="{StaticResource ApplicationForegroundThemeBrush}"
                                           FontSize="25"
                                           Margin="5" />


                                </Grid>
                            </DataTemplate>
                        </GroupStyle.HeaderTemplate>

                        <!--<GroupStyle.Panel>
                            <ItemsPanelTemplate>
                                <VariableSizedWrapGrid Orientation="Vertical" Height="400" />
                            </ItemsPanelTemplate>
                        </GroupStyle.Panel>-->
                    </GroupStyle>
                </GridView.GroupStyle>

            </GridView>

        </Grid>

        <!--Progress indicator-->
        <ProgressRing Visibility="{Binding BusyIndicatorVisibility}"
                      IsActive="{Binding IsBusy}"
                      Width="50"
                      Height="50"
                      Grid.Row="1"
                      Foreground="White"/>


        <!--Here we define our layout for the items list.
        We need a way to switch categories, and bind the GridView to that category's items.-->
        <!--
        <Pivot x:Name="CategoryPivot"
               ItemsSource="{Binding Categories}"
               SelectedItem="{Binding SelectedCategory, Mode=TwoWay}"
               Margin="12"
			   Grid.Row="1">


            -->
        <!--Define the header template-->
        <!--
            <Pivot.HeaderTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding Name}"/>
                </DataTemplate>
            </Pivot.HeaderTemplate>

            -->
        <!--Define the item template. In this case, it is just a grid view.-->
        <!--
            <Pivot.ItemTemplate>
                <DataTemplate>
                    <Grid>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>

                        <Button Grid.Row="0" 
                                Command="{Binding DataContext.AddItemCommand, ElementName=ItemsViewPage }">
                            <SymbolIcon Symbol="Add" />
                        </Button>
                        
                    </Grid>
                </DataTemplate>
            </Pivot.ItemTemplate>
        </Pivot>-->

        <VisualStateManager.VisualStateGroups>
            <VisualStateGroup x:Name="VisualStateGroup">
                <VisualState x:Name="VisualStatePhone">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="0"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>

                        <Setter Target="SearchTextBox.HorizontalAlignment" Value="Stretch" />
                        <Setter Target="SearchTextBox.Width" Value="auto" />
                    </VisualState.Setters>
                </VisualState>

                <VisualState x:Name="VisualStateTablet">
                    <VisualState.StateTriggers>
                        <AdaptiveTrigger MinWindowWidth="1024"/>
                    </VisualState.StateTriggers>

                    <VisualState.Setters>
                   

                    </VisualState.Setters>
                </VisualState>
            </VisualStateGroup>
        </VisualStateManager.VisualStateGroups>
    </Grid>
</Page>
